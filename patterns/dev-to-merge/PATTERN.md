---
name = "dev-to-merge"
description = "Legacy alias for dev2merge. Redirects to the deterministic pipeline."
allowed-tools = "Bash, Read, Edit, Write, Grep, Glob, Task"
tier = "tier-3-complex"
version = "0.2.0"
---

# Dev-to-Merge (Alias)

This pattern is an alias for `dev2merge`. All logic lives in `dev2merge`.

## INCLUDE ../dev2merge/PATTERN.md
