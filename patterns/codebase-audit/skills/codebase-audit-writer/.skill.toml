[skill]
name = "codebase-audit-writer"
version = "0.2.0"

[agent]
tier = "tier-4-critical"
max_turns = 60
tools = [{ tool = "claude-code", provider = "anthropic", model = "claude-opus-4-6", thinking_budget = "xhigh" }]
skip_context = ["patterns/", "skills/"]
extra_context = ["CLAUDE.md"]
